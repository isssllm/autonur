'use client';
import { ArrowLeft, ArrowRight } from 'lucide-react';
import { useRef } from 'react';
type CarCard = { id:string; name:string; photo:string; year:number; transmission:string };
export default function CityCars({ items }: { items: CarCard[] }) { const ref=useRef<HTMLDivElement>(null); const scroll=(n:number)=>ref.current?.scrollBy({left:n,behavior:'smooth'}); return <div><div className="mb-5 flex justify-end gap-2"><button aria-label="Назад" onClick={()=>scroll(-360)} className="focus-ring grid h-10 w-10 place-items-center rounded-full border border-black/10 bg-white dark:border-white/10 dark:bg-neutral-900"><ArrowLeft size={17}/></button><button aria-label="Вперёд" onClick={()=>scroll(360)} className="focus-ring grid h-10 w-10 place-items-center rounded-full border border-black/10 bg-white dark:border-white/10 dark:bg-neutral-900"><ArrowRight size={17}/></button></div><div ref={ref} className="flex snap-x snap-mandatory gap-4 overflow-x-auto pb-4 [scrollbar-width:none] [&::-webkit-scrollbar]:hidden">{items.map((car)=><article key={car.id} className="surface min-w-[82%] snap-start overflow-hidden sm:min-w-[48%] lg:min-w-[31%]"><div className="aspect-[4/3] overflow-hidden bg-neutral-200 dark:bg-neutral-900"><img src={car.photo} alt={car.name} className="h-full w-full object-cover transition duration-700 hover:scale-105"/></div><div className="p-5"><div className="text-lg font-semibold">{car.name}</div><div className="muted mt-2 flex gap-3 text-sm"><span>{car.transmission === 'AUTOMATIC' ? 'Автомат' : 'Механика'}</span><span>•</span><span>{car.year}</span></div></div></article>)}</div></div> }
