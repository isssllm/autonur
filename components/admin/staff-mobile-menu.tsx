'use client';
import Link from 'next/link';
import { Menu, X } from 'lucide-react';
import { useState } from 'react';
import LogoutButton from '@/components/logout-button';
export default function StaffMobileMenu({items}:{items:string[][]}){const[open,setOpen]=useState(false);return <><button aria-label="Открыть меню" onClick={()=>setOpen(true)} className="focus-ring grid h-10 w-10 place-items-center rounded-xl border border-black/10 bg-white md:hidden dark:border-white/10 dark:bg-neutral-900"><Menu size={18}/></button>{open&&<div className="fixed inset-0 z-50 md:hidden"><button aria-label="Закрыть" className="absolute inset-0 bg-black/40" onClick={()=>setOpen(false)}/><aside className="relative z-10 h-full w-[84%] max-w-sm bg-white p-4 dark:bg-neutral-950"><div className="flex items-center justify-between"><div className="font-semibold">Autonur</div><button onClick={()=>setOpen(false)} className="grid h-10 w-10 place-items-center rounded-xl border border-black/10 dark:border-white/10"><X size={18}/></button></div><nav className="mt-6 space-y-1">{items.map(([href,label])=><Link onClick={()=>setOpen(false)} href={href} key={href} className="block rounded-xl px-3 py-3 text-sm text-neutral-700 hover:bg-black/5 dark:text-neutral-200 dark:hover:bg-white/5">{label}</Link>)}</nav><div className="mt-5 border-t border-black/5 pt-5 dark:border-white/10"><LogoutButton redirectTo="/"/></div></aside></div>}</>}
