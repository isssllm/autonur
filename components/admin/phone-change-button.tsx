'use client';
import { useState } from 'react';
export default function PhoneChangeButton({userId,current,city}:{userId:string;current:string;city:string}){const[open,setOpen]=useState(false);const[phone,setPhone]=useState(current);const[msg,setMsg]=useState('');const[busy,setBusy]=useState(false);async function save(){setBusy(true);setMsg('');const r=await fetch('/api/admin/users/phone',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({userId,phone,city})});const d=await r.json();setMsg(r.ok?'Номер изменён.':(d.error||'Ошибка'));setBusy(false);if(r.ok)setOpen(false)}return <>{<button onClick={()=>setOpen(true)} className="rounded-full border border-black/10 px-3 py-1.5 text-xs dark:border-white/10">Изменить номер</button>}{open&&<div className="fixed inset-0 z-50 grid place-items-center bg-black/40 p-4"><div className="surface w-full max-w-md p-6"><h2 className="text-xl font-semibold">Изменить номер</h2><p className="muted mt-2 text-sm">Старый номер: {current}</p><input autoFocus value={phone} onChange={e=>setPhone(e.target.value)} className="focus-ring mt-5 w-full rounded-2xl border border-black/10 bg-white px-4 py-3 dark:border-white/10 dark:bg-neutral-900" placeholder="+7 700 000 00 00"/>{msg&&<div className="mt-3 text-sm text-red-600">{msg}</div>}<div className="mt-5 flex justify-end gap-2"><button onClick={()=>setOpen(false)} className="rounded-full border px-4 py-2 text-sm">Отмена</button><button disabled={busy} onClick={save} className="rounded-full bg-orange-500 px-4 py-2 text-sm font-semibold text-white">{busy?'Сохраняем…':'Сохранить'}</button></div></div></div>}</>}
