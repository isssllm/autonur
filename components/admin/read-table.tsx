import type { ReactNode } from 'react';
export default function ReadTable({headers,rows}:{headers:string[];rows:(string|ReactNode)[][]}){return <div className="surface overflow-hidden"><div className="overflow-x-auto"><table className="min-w-[720px] w-full text-left text-sm"><thead className="bg-black/[.025] text-xs text-neutral-500 dark:bg-white/[.035]"><tr>{headers.map(h=><th key={h} className="px-4 py-3 font-medium">{h}</th>)}</tr></thead><tbody className="divide-y divide-black/5 dark:divide-white/10">{rows.map((row,i)=><tr key={i}>{row.map((cell,j)=><td key={j} className="px-4 py-4 align-top">{cell}</td>)}</tr>)}</tbody></table></div></div>}
